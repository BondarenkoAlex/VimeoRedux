import React from 'react';
import PropTypes from 'prop-types';

function Loading() {
  return (
    <div>Loading...</div>
  );
}

Loading.propTypes = {};
Loading.defaultProps = {};

export default Loading;